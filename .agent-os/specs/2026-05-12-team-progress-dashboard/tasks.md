<!-- Generated by AI on 05/12/2026 -->

# Team Progress Dashboard — Task Breakdown

> Derived from [spec.md](spec.md). Tasks are ordered by dependency.

---

## Task 1: Add backend DTOs and service method

- [ ] Add `TeamProgressDto` and `TeamMemberDto` records to `Models/Dtos.cs`
- [ ] Add `GetTeamProgressAsync()` to `IAdminService`
- [ ] Implement `GetTeamProgressAsync()` in `AdminService`:
  - Fetch all participants
  - Group by `Team` (empty → "Unassigned")
  - For each participant, fetch evidence for current level to determine `LastEvidenceDate`
  - Calculate `DaysAtCurrentLevel` from `LastAdvancementAt` or `CreatedAt`
  - Determine `IsStalled` (14-day threshold)
  - Build per-team DTOs with level counts
- [ ] Add stalled threshold constant (`StalledDaysThreshold = 14`)

---

## Task 2: Add backend controller endpoint

- [ ] Add `GET /api/admin/team-progress` to `AdminController`
- [ ] Protect with existing `AdminOnly` policy (already on controller)
- [ ] Return `List<TeamProgressDto>` from service

---

## Task 3: Add backend unit tests

- [ ] Test multiple teams with mixed badge levels — verify correct level counts
- [ ] Test stalled detection — participant at level 14+ days with no recent evidence → `IsStalled = true`
- [ ] Test active detection — participant with recent evidence → `IsStalled = false`
- [ ] Test empty `Team` field grouped as "Unassigned"
- [ ] Test empty participant list returns empty result
- [ ] Verify all tests pass via `dotnet test`

---

## Task 4: Add frontend types and API hook

- [ ] Add `TeamProgressDto` and `TeamMemberDto` interfaces to `types/index.ts`
- [ ] Add `useTeamProgress()` hook to `api/adminApi.ts`

---

## Task 5: Build TeamCard component

- [ ] Create `components/TeamCard.tsx` — accordion-style card
- [ ] Render team name, member count, stacked level bar in summary
- [ ] Render expandable member table with columns: Name, Level, Days at Level, Last Evidence, Status
- [ ] Link participant names to `/admin/participant/:userId`
- [ ] Highlight stalled members with warning chip

---

## Task 6: Build TeamProgressPage

- [ ] Create `pages/TeamProgressPage.tsx`
- [ ] Add team name search/filter (client-side)
- [ ] Render list of `TeamCard` components with filtered data
- [ ] Handle loading and empty states

---

## Task 7: Wire up routing and navigation

- [ ] Add route `/admin/teams` → `TeamProgressPage` in `App.tsx` (protected by admin role)
- [ ] Add "Teams" navigation link in `NavBar` (visible to admins)

---

## Task 8: Add frontend unit tests

- [ ] Test `TeamCard` renders team name, member count, level bar
- [ ] Test `TeamCard` shows stalled indicator for stalled members
- [ ] Test `TeamProgressPage` search filter narrows team list
- [ ] Verify all tests pass via `npm test`

---

## Task 9: End-to-end verification

- [ ] Run `dotnet test` — all backend tests pass
- [ ] Run `npm test` — all frontend tests pass
- [ ] Run `npm run lint` — no lint errors
- [ ] Start backend + frontend, verify page loads with dev bypass auth

---

## Dependency Order

```
Task 1  (DTOs + service)     — unblocks Tasks 2, 3
Task 2  (endpoint)           — unblocks Task 9
Task 3  (backend tests)      — depends on Task 1
Task 4  (FE types + hook)    — unblocks Tasks 5, 6
Task 5  (TeamCard)           — depends on Task 4
Task 6  (TeamProgressPage)   — depends on Tasks 4, 5
Task 7  (routing + nav)      — depends on Task 6
Task 8  (FE tests)           — depends on Tasks 5, 6
Task 9  (e2e verification)   — depends on all
```
